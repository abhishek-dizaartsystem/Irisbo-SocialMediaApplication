package com.example.sociamediaapplication.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.example.sociamediaapplication.R
import kotlin.random.Random

val BabyPlums = FontFamily(
    Font(R.font.babyplums)
)

val BrownieStencil = FontFamily(
    Font(R.font.browniestencil)
)

val HappySwirly = FontFamily(
    Font(R.font.happyswirly)
)

val LoveDays = FontFamily(
    Font(R.font.lovedays)
)

val ShinyCrystal = FontFamily(
    Font(R.font.shinycrystal)
)

val Akronim = FontFamily(
    Font(R.font.akronim)
)

val SansSeriff = FontFamily.Serif